// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Custom apex domain served by GitHub Pages (see public/CNAME).
  site: 'https://haliburtonco.com',
});
